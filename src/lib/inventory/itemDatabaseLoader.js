/**
 * Item Database Loader
 * 
 * Browser-compatible loader for the item database.
 * In React components, import this instead of the JSON file directly.
 */

import itemDatabaseRaw from '../../data/items/itemDatabase.json';

export const itemDatabase = itemDatabaseRaw;

export default itemDatabase;
